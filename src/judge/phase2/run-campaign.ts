/** Run one black-box advisory Phase-2 campaign and materialize its artifacts. */
import {createHash} from "node:crypto";import {mkdir,writeFile} from "node:fs/promises";import {join} from "node:path";import {stringify} from "yaml";import {loadPhase2Case} from "./load-cases.js";import {aggregatePatterns,extractObservations} from "./patterns.js";import type {Phase2Analyst} from "./analyst.js";import type {Phase2DeveloperPack} from "./types.js";
export interface RunPhase2Input{campaignId:string;projectId:string;sutFingerprint:string;viewDirs:readonly string[];outputDir:string;analyst:Phase2Analyst}
async function put(path:string,value:unknown){const body=stringify(value,{lineWidth:0});await writeFile(path,body);return{path,bytes:Buffer.byteLength(body),sha256:createHash("sha256").update(body).digest("hex")}}
/** Execute deterministic analysis + agentic recommendation design; no experiment execution. */
export async function runPhase2Campaign(x:RunPhase2Input):Promise<{artifactDir:string;developerPackSha256:string;pack:Phase2DeveloperPack}>{const cases=await Promise.all(x.viewDirs.map(loadPhase2Case));const valid=cases.filter(c=>c.validForAgentLearning),platform=cases.filter(c=>!c.validForAgentLearning);const observations=extractObservations(cases),patterns=aggregatePatterns(observations);const recommendations=await x.analyst.recommend({campaignId:x.campaignId,projectId:x.projectId,patterns});const pack:Phase2DeveloperPack={schemaVersion:1,campaignId:x.campaignId,projectId:x.projectId,sutFingerprint:x.sutFingerprint,memberRunIds:cases.map(c=>c.runId),validAgentRuns:valid.length,platformFailures:platform.length,patterns,recommendations,generatedAt:new Date().toISOString()};await mkdir(x.outputDir,{recursive:true});const files=[];files.push(await put(join(x.outputDir,"campaign.yaml"),{schema_version:1,campaign_id:x.campaignId,project_id:x.projectId,sut_fingerprint:x.sutFingerprint,members:cases.map(c=>({run_id:c.runId,valid_for_agent_learning:c.validForAgentLearning,failure_owner:c.failureOwner,reward:c.reward}))}));files.push(await put(join(x.outputDir,"patterns.yaml"),{schema_version:1,observations,patterns}));const dev=await put(join(x.outputDir,"developer-pack.yaml"),pack);files.push(dev);files.push(await put(join(x.outputDir,"experiment-plans.yaml"),{schema_version:1,plans:recommendations.map(r=>r.experimentPlan)}));files.push(await put(join(x.outputDir,"platform-report.yaml"),{schema_version:1,cases:platform.map(c=>({run_id:c.runId,failure_owner:c.failureOwner,narrative:c.narrative}))}));const manifest={schema_version:1,campaign_id:x.campaignId,files:files.map(f=>({path:f.path.split("/").pop(),bytes:f.bytes,sha256:f.sha256}))};await put(join(x.outputDir,"manifest.json"),manifest);return{artifactDir:x.outputDir,developerPackSha256:dev.sha256,pack}}
