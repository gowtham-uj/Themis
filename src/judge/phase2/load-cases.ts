/** Load published Phase-1 views into black-box Phase-2 case records. */
import {readFile} from "node:fs/promises";import {join} from "node:path";import {parseAllDocuments} from "yaml";import type {Phase2Case} from "./types.js";
async function json(path:string):Promise<Record<string,unknown>|null>{try{return JSON.parse(await readFile(path,"utf8")) as Record<string,unknown>}catch{return null}}
function n(v:unknown):number|null{return typeof v==="number"&&Number.isFinite(v)?v:null}
/** Load one immutable Phase-1 archive view. */
export async function loadPhase2Case(viewDir:string):Promise<Phase2Case>{const y=await readFile(join(viewDir,"judge","evalJudge.yaml"),"utf8");const d=parseAllDocuments(y)[0]?.toJS() as Record<string,unknown>|undefined;if(!d)throw new Error(`invalid evalJudge: ${viewDir}`);const validity=(d.eval_validity??{}) as Record<string,unknown>;const verdict=(d.verdict??{}) as Record<string,unknown>;const metrics=await json(join(viewDir,"eval_lifecycle_logs","run-metrics.json"));const run=await json(join(viewDir,"eval_lifecycle_logs","run.json"));const quality=await json(join(viewDir,"judge","quality-report.json"));const meas=(metrics?.measurements??{}) as Record<string,{value?:unknown}>;const mv=(k:string)=>n(meas[k]?.value)??n(metrics?.[k]);return{runId:String(d.eval_id??run?.runId??""),validForAgentLearning:validity.valid_for_agent_learning!==false,failureOwner:String(validity.failure_owner??"none"),reward:n(d.official_reward),agent:String(d.agent_under_evaluation??run?.agentId??"unknown"),model:typeof run?.model==="string"?run.model:null,tokens:mv("tokens_used"),wallTimeMs:mv("wall_clock_ms"),toolCalls:mv("tool_calls")??n(metrics?.toolCallCount),narrative:String(d.narrative??""),improvements:Array.isArray(d.improvements)?d.improvements as Record<string,unknown>[]:[],qualityPassed:quality?.passed===true};}
