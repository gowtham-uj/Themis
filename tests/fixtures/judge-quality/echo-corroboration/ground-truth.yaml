# Ground truth for eval-echo-corr-0007.
# acceptable_verdicts are enum SETS; the YAML lists below are loaded into ReadonlySet.
must_find:
  - finding: The null-recipient fix is in src/dispatch.ts.
    ref: diff:src/dispatch.ts#hunk-1
  - finding: Kratos and logos cite the same single ref, so the fix is one observation.
    ref: report:minos#round2
  - finding: Verifier check test_dispatch_null_recipient passed.
    ref: verifier:1

must_not_claim:
  - The fix is corroborated by two independent observations.
  - Two distinct refs support the null-guard claim.

acceptable_verdicts:
  approach: [principled]
  integrity: [clean]
  competence: [4, 5]
  reconciliation: [consistent]

min_confidence: high
max_confidence: high
