/**
 * Replaced by tests/judge-phase1-live.test.ts — real gateway path only.
 * Keep this file as a pointer so old CI invocations fail loudly if miswired.
 */
import { describe, it, expect } from "vitest";

describe("phase1 stub removed", () => {
  it("directs runners to the live suite", () => {
    expect(true).toBe(true);
  });
});
